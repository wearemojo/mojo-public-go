on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2.4.0

    - uses: actions/setup-go@v2.1.5
      with:
        go-version: '^1.17'

    - run: go build -v ./...

    - run: go test -v ./...
