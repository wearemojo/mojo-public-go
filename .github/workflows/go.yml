on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5.0.0

    - uses: actions/setup-go@v5.5.0
      with:
        go-version-file: 'go.mod'

    - run: go build -v ./...

    - run: go test -v ./...
