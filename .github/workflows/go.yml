on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4.1.6

    - uses: actions/setup-go@v5.0.1
      with:
        go-version: '^1.22.0'

    - run: go build -v ./...

    - run: go test -v ./...
