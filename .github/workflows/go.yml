on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - uses: actions/setup-go@v3.0.0
      with:
        go-version: '1.18.0-rc.1'

    - run: go build -v ./...

    - run: go test -v ./...
